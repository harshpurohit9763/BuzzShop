import 'package:flutter/material.dart';

final Decor1LoginPage = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.only(
        topLeft: Radius.circular(20), topRight: Radius.circular(20)));

final Decor1LoginPageButton = BoxDecoration(
    color: Color.fromARGB(255, 76, 82, 165),
    borderRadius: BorderRadius.circular(10));

final Decor1SignUpPage = BoxDecoration(
  color: Colors.white,
);
